"""
routes/dashboard.py — HARROW Control Centre dashboard.

Endpoints:
- GET /              — serve index.html via Jinja2
- GET /stream/{id}   — SSE stream for live terminal output
"""

# Phase 5: implement dashboard route, SSE streaming
