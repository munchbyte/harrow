"""
routes/hitl_routes.py — HARROW HITL gate endpoints.

Endpoints:
- GET  /gates              — list pending HITL gates
- POST /gates/{id}/resolve — approve a gate, job resumes
- GET  /approve/{token}    — One-Touch Receipt signed URL approval
"""

# Phase 1: implement hitl route handlers
# Phase 4: implement One-Touch Receipt /approve/{token}
